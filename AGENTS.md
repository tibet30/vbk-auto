# AGENTS 协作规范

## 代码规范

- 每个文件默认不超过 `350` 行。
- 上下浮动不超过 `50` 行（即约 `300~400` 行）时，如改动不大可暂不拆分文件。
- 超过该范围时应优先拆分文件，提升可读性与维护性。

## 设计规范

- 视觉与交互规范见 [DESIGN.md](/Users/cisco/Documents/vbk-auto/DESIGN.md)。

## AI 实施协作规范

本节为本仓库 AI 协作的强约束（binding rules），除非用户显式覆盖，否则对所有后续改动一律适用。

### 当前临时模式：Codex 单执行器

- 自 2026-08-13 起，Pi 与 Claude Code 的强制实施、审查和纠错流程暂停，当前仅由 Codex 执行。
- Codex 负责需求理解、只读调研、bounded 实施计划、验收门、代码与测试实施、问题修复、独立验证和最终交付。
- 下方第 1～3、6 节旧流程已注释，仅供后续恢复时参考，当前不生效。
- 第 4 节工作区保护和第 5 节高风险操作红线继续生效。

<!-- 暂停：Pi / Claude Code 强制分工与 Codex 禁止实施规则
### 1. Codex 职责边界

- **拥有（owns）**：需求理解、只读调研、bounded 实现规划、任务切片、风险与**预先定义的验收门（acceptance gates：关键用例 + 期望结果 + 失败标准）**、进度协调、最终独立审查与验证。
- **禁止直接编辑**：源代码、测试、配置文件、实施类文档。Codex 仅作为规划者与协调者，不亲自下场写实现，也不在审查后自行打补丁。

### 2. 实施分工（默认流程）

- 任何代码改动前，Codex 必须先产出**有边界（bounded）的实施计划 + 显式验收门**，然后**按顺序**调度两个执行器：
  1. **Pi（首执行器，承担完整实现）**：在 Codex 划定的边界与验收门内，承担该任务的**全部实现工作**——含多步推理、跨文件改动、集成、边界处理与命令验证，直到产出可被审查的实现 diff。Pi 不再被限于"首改、首跑、首查"等机械化片段；常规复杂实现、跨文件集成、端到端改动默认都属于 Pi 的实现范围。
  2. **Claude Code（次执行器，仅在 Pi 之后启动）**：负责对 Pi 的完整实现做**全量审查（review）并按需打代码纠错补丁（corrective changes）**，覆盖代码质量、边界一致性、测试覆盖、与既有约定的对齐；不再承担"首改实现"。
- Codex 自身不进入实施环节；不得把常规实现默认推给 Claude Code，也不得让 Pi 仅跑机械化首轮。

### 3. 验收门与独立核验

- Codex 在调度 Pi 之前必须显式列出**验收门（acceptance gates）**：关键测试用例、期望结果、失败标准与判定方式；这些门控同时约束 Pi 的实现与 Claude Code 的审查。
- Pi 完成实现 → Claude Code 完成审查与必要纠错后，Codex 必须**独立检视 diff** 并运行相关检查（lint / type / test / build / 自定义验收门）。
- 发现问题时，Codex 可向 Pi 或 Claude Code 发送**精确的纠错指令**，但**不得自行打补丁**；代码层面的修补只能由执行器完成。
- 最终验收以 Codex 的独立核验为准。
-->

### 4. 执行器行为约束

- 保留工作区中与本任务无关的脏改动（unrelated dirty worktree changes），不得清理或覆盖。
- 严格在分配的文件范围内作业，禁止越权改动未授权文件。

### 5. 高风险操作红线

- 未经用户**显式授权**，禁止执行以下任一操作：
  - `git commit` / `git push` / 合并分支
  - 部署、上线、发布
  - 任何破坏性操作（删除、重置、强制覆盖）
  - 凭据持久化、外部提交、对外投递
- 需要授权时必须先列出动作清单并等待确认。

<!-- 暂停：执行器不可用时的阻塞规则
### 6. 执行器不可用

- 若 Pi 或 Claude Code 任一不可用，Codex 必须**显式上报阻塞**，不得为了"完成任务"而自行以编辑代码的方式顶替执行器。
-->
