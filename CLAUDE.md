# CLAUDE 工作规范

## 执行模式

- **默认直接执行**：不要问"是否同意"、"是否要继续"、"要我做吗"。
- **真正卡住才问**：仅当缺少关键信息（项目 ID、路径、参数）无法继续时才问。
- **失败就报告**：命令报错、测试不过、构建失败——停下来贴出错误与定位，不要猜测下一步。
- **不确定时按最合理方案执行**：在结果里说明假设，不要反复确认。

## 不可逆操作

下列动作**直接做**，但完成后必须告知结果：

- `git commit`、`git push`（除 `--force` 到 main/master）
- 安装/升级依赖（`npm install`、`yarn add`）
- 删除 `node_modules`、`dist`、`tmp/`、`.tmp-*.ts` 等构建产物
- 跑测试套件、lint、typecheck
- 重启 Electron / Playwright 调试服务

下列动作**先停下报告**：

- `git push --force`、`git reset --hard`、删除远程分支
- 修改 `.env`、密钥存储、VBK Cookie 等敏感文件
- 调用任何"提审 / 发布 / 自动化落库"接口（产品规范要求人工发布）
- 改动 `package.json` 启动脚本或 CI 配置

## 项目常识

- **项目**：VBK Desktop（macOS 桌面工作台，Electron + React + MiniMax + Playwright）。
- **自动化边界**：只跑"安全录入"阶段，**不要**触发 VBK 提审/发布。
- **密钥与账号**：MiniMax 密钥、VBK Cookie 仅存本机加密存储，禁止写入仓库或日志。
- **调试入口**：`npm run pi:itinerary -- <projectId> [cdpPort]`，默认 CDP 端口 `9539`。
- **代码规范**：每个文件默认 ≤ 350 行（±50 行），超过优先拆分。详见 `AGENTS.md`。
- **设计规范**：视觉与交互见 `DESIGN.md`。

## 工作流偏好

- 修改前先 `read` 目标文件，确认现状再改。
- 跨多文件改动时一次性 `edit` 多处，不要来回多次调用。
- 跑测试用 `npm test` 或 `npm run check`，不要逐个手敲命令。
- 输出结果时给文件路径 + 行号区间，便于复查。
- 中文回复优先（与项目语境一致）。

## 不要做的事

- 不要在回复里堆砌"我将..."、"让我..."、"Sure, I'll..."等空话。
- 不要为已知的命令语法 / 项目结构重复问用户。
- 不要把临时调试文件（`tmp-*`、`*.tmp-*`）提交到 git。
- 不要修改 `PRODUCT.md` 中已固化的产品定位、品牌承诺、能力边界。