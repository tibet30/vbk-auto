/**
 * 产品 schema 聚合 re-export：把 zod schema 定义 + 解析 / 选图 / 下拉匹配等 helper 一次暴露。
 * 实际逻辑见 ./schema-definitions.ts / ./schema-functions.ts。
 */

export * from "./schema-definitions.js";
export * from "./schema-functions.js";
